name: CI/CD Pipeline

on:
  push:
    branches: [ "main" ]

jobs:
  build-and-test:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3
    
    - name: Set up JDK 17
      uses: actions/setup-java@v3
      with:
        java-version: '17'
        distribution: 'temurin'
        
    - name: Run Tests with test profile
      run: ./mvnw test
      env:
        SPRING_PROFILES_ACTIVE: test

    - name: Login to Docker Hub
      uses: docker/login-action@v2
      with:
        username: ${{ secrets.DOCKER_USERNAME }}
        password: ${{ secrets.DOCKER_PASSWORD }}
        
    - name: Build and push Docker image
      uses: docker/build-push-action@v4
      with:
        context: .
        push: true
        tags: ${{ secrets.DOCKER_USERNAME }}/spring-item-api:latest

  # DEPLOY-VAIHE KOMMENTOITU POIS, KOSKA CPOUTA-PALVELINTA EI OLE VIELÄ PYSTYTETTY
  # deploy:
  #   needs: build-and-test
  #   runs-on: ubuntu-latest
  #   steps:
  #   - name: Deploy to cPouta VM
  #     uses: appleboy/ssh-action@master
  #     with:
  #       host: ${{ secrets.HOST }}
  #       username: ${{ secrets.USERNAME }}
  #       key: ${{ secrets.KEY }}
  #       script: |
  #         cd /home/centos/project
  #         export SPRING_PROFILES_ACTIVE=prod
  #         export DOCKER_USERNAME=${{ secrets.DOCKER_USERNAME }}
  #         docker compose pull
  #         docker compose up -d